syntax = "proto3";
package testnet.aggregation;

option go_package = "testnet/x/aggregation/types";

message Price {
  
  string source = 1; 
  uint64 timestamp = 2; 
  uint64 price = 3; 
  uint64 id = 4; 
}
